# Query: createEmailPasswordSession
# ContextLines: 1

2 results - 1 file

src\lib\appwrite\api.ts:
   70  //     try {
   71: //         const session = await account.createEmailPasswordSession(user.email, 
   72  //             user.password);

  181    try {
  182:     const session = await account.createEmailSession(user.email, user.password);
  183  
